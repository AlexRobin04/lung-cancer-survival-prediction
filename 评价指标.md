2.8  评价指标
2.8.1  C-index
本文选用Harrell C-index作为评价模型性能的核心指标，它的作用是衡量模型给出的病例级别风险评分，跟患者实际生存结局之间在排序上的一致程度。在生存分析这个场景当中，单纯去看分类准确率其实并不太合适，因为生存数据天然会带有删失（也就是censoring）的情况。所谓删失，指的是部分患者的真实生存时间实际上是未知的，有可能是因为研究结束时患者仍未发生所关注的事件，也有可能是患者在中途失访了。而C-index恰恰能够在这样一种部分信息有所缺失的条件下，去评估模型对患者风险高低所进行的排序能力。
具体来说，C-index的计算思路是这样的：从队列里找出所有“可比较的患者对”，所谓可比较，就是根据两人的删失状态和生存时间，能够判断出谁先发生事件、谁后发生事件。对于这样一对患者i和j，假设模型给i预测的风险分数是ri，给j预测的是rj，两人的真实生存时间分别是Ti和Tj，删失指示分别是δi和δj。如果在所有可比较对当中，模型给出的风险排序和实际结局的先后顺序一致的占比越高，C-index就越接近1。要是模型排序完全随机，那就和抛硬币差不多，C-index落在0.5附近。如果模型不仅没有排序对，还老是逆着来，那C-index就会往0的方向走。遇到两人风险分数打平的情况，按约定给0.5的权重，相当于不偏不倚各算一半。C-index的取值范围从0到1，0.5是一条常识性的基准线，比0.5高说明模型多少学到了一些排序信号，越接近1说明模型的排序能力和真实临床结局越吻合。考虑到本文最终要回答的问题是在部署队列上对不同患者的风险进行可靠排序，所以把C-index定为主指标，其他的指标更多是辅助参考。
2.8.2  Log-rank p-value
Log-rank检验是生存分析当中一种常用的非参数方法，它的用途是比较两条或者多条Kaplan–Meier曲线所对应的人群，看看它们整体的生存分布是否相同。该检验的基本思路是：在每一个事件时间点上，去比较各组实际观察到的事件发生数量，与“各组生存分布相同”这个原假设之下所期望的事件发生数量，然后把两者之间的差异汇总成一个近似服从χ²分布的统计量，并据此计算出一个双侧检验的p值，也就是Log-rank p-value。这个检验的原假设是各组的生存分布没有差异；当计算出来的p值小于事先设定的显著性水平（比如0.05）时，就可以认为组间生存差异具有统计学上的意义。需要说明的是，Log-rank检验所回答的是“曲线整体上是否存在差异”这个问题，它并不能刻画连续型的风险评分之间那种高低排序的关系。同时，检验的结果会受到样本量大小、实际发生的事件数量以及删失情况等因素的影响，在小样本的情况下，检验的功效可能不足，这会导致p值偏大。在本文的工作当中，在按照风险评分将病例分成两组之后，会对这两组的KM曲线进行Log-rank检验，这样做主要是为了辅助说明，经过分层之后所观察到的生存差异是否能够在统计学上被检测出来。
2.8.3  验证损失loss
验证损失也是日志层的一个重要观测维度。每一轮训练结束之后，模型在验证集上计算一次损失函数的值，把这个值随时间变化的曲线画出来，能很直观地看到模型有没有正常收敛、有没有出现过拟合的趋势。如果训练损失一直在降、验证损失却掉头开始往上走，那就是一个比较明确的过拟合信号，模型开始在训练集上死记硬背了，没在学真正泛化的东西。在本文的实验设置里，不同MIL结构的验证损失曲线形态差异还挺大的，有的模型收敛得比较平稳，比如DSMIL的验证损失从最优到最终几乎没怎么变，而像WiKG这种训练过程中损失波动就比较剧烈，best val loss和final val loss之间拉开了一倍多的差距，说明这种结构对这个数据集来说可能更容易过拟合或者说训练稳定性偏弱一些。
2.8.4  Hazard Ratio（HR）
Hazard Ratio（也就是风险比，简称HR）是在Cox比例风险模型的框架之下，用来刻画某一个二分类因素相对于参照组的瞬时风险倍数的指标。设回归系数为β，那么HR就被定义为HR = exp(β)。当HR大于1的时候，表示该组的事件发生风险要高于参照组；当HR小于1的时候，表示风险更低一些；而当HR等于1的时候，则表示两组的风险相当。在实践当中，通常需要同时报告HR的点估计值、95%的置信区间以及系数检验所对应的p值，以此来反映效应的大小以及它所包含的不确定性。需要指出的是，HR的解释是依赖于比例风险等模型假设的。单变量Cox模型所得到的HR，反映的是该分组变量与生存结果之间的边际关联，这与经过多因素调整之后所得到的HR含义是不同的。Log-rank检验和HR这两个指标，常常会结合同一个分层口径来一并报告：前者侧重的是曲线整体上是否存在显著的差异，后者侧重的则是风险比的一个定量大小，两者之间是互补的关系。在本文的工作当中，会对分层变量去拟合一个单变量的Cox模型，然后报告较高风险组相对于参照组的HR值，以及它的置信区间和p值。