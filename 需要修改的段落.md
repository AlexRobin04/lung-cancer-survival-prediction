在训练的时候，通过 `args.model_type == "RRTMIL"` 这个分支，在 `core_utils.py` 文件当中对模型进行实例化，并运用 Adam 优化器以及交叉熵损失来开展优化。在超参数方面，所涉及的设置包括了：投影维度设为 256、编码器层数为 2 层、注意力头数量为 8、区域个数为 8、Dropout 取值为 0.25，分类头的输出是 4 档风险等级。该模型在部署所用随访队列上的 Harrell C-index（取该平台 `predictions.json` 中该模型类型下**队列 C-index 最高的代表任务**）为 **0.4178**（bootstrap 95% CI：**0.2926–0.5522**），与表 4 一致。验证损失从最优的 1.3278 上升至最终的 1.6551，涨幅约 25%，提示在样本量有限时 Transformer 式区域聚合存在一定过拟合风险。

在训练阶段，通过 `args.model_type == "AMIL"` 这个分支来触发模型的实例化操作，所使用的超参数与其他基线模型保持一致。该模型在队列上的 Harrell C-index 为 **0.4623**（95% CI：**0.3344–0.5917**，与表 4 一致），在五条基线单模中高于 RRTMIL，低于 DSMIL，与 WiKG 接近。验证损失从 1.3377 到 1.3392 几乎不变，训练过程平稳，可作为集成支路中稳定的注意力基线。

DSMIL 的队列 C-index 为 **0.5115**（95% CI：**0.3778–0.6589**），在五条单模中位居前列；验证损失从最优到最终保持 1.3596 不变，收敛轨迹干净，适合作为集成中的判别式 MIL 证据源。

S4MIL 的队列 C-index 为 **0.5623**（95% CI：**0.3900–0.7028**），与 **EnsembleDecision 在「各自队列 C-index 最高代表任务」上并列最高**（见表 4）。在当前部署与默认**队列蒸馏对齐**策略下，二者 Cox HR、Log-rank p 与六模型 KM 格内展示一致（表 5），体现集成输出与最强排序支路在生存分层口径上的对齐。WSI 预后信号分散于大量图块、需要长程依赖建模的设定，与 S4MIL 作为关键支路相吻合。

WiKG 的队列 C-index 为 **0.4490**（95% CI：**0.2912–0.6185**）。验证损失从 1.3169 跳升至 2.9378，训练曲线波动较大，但其图结构支路在集成中与注意力类、序列类特征形成互补，有利于决策层融合时的证据多样性。

每种模型在数据加载阶段，训练与验证样本自预提取的双尺度 H5 特征库中按顺序或随机策略遍历，是否加权依划分与配置而定。20× 与 10× 两档特征与 patch 提取倍率一致，模型前向同时消费两路张量以形成多尺度证据。特征载入后转为浮点类型并在 CPU 或 GPU 上统一计算。本文不在 patch 像素域以随机翻转等为主做增广，而依赖队列内病例异质性与 MIL 聚合结构提升表达；若特征缺失，可由全切片经在线特征提取补全后进入同一训练管线。训练主循环在每个 epoch 内依次执行袋级前向、损失回传、优化器步进与验证评估；**验证阶段以分类损失及与生存排序相关的验证 C-index 等为主**，用于监控代理目标与随访排序之间是否脱节。集成任务在载入五路基线检查点后，进入决策融合与写盘路径。通过上述环境与协议，本文获得了五类 MIL 基线在 TCGA-LUSC 子风险任务上的成套训练记录，以及集成方法 **EnsembleDecision** 在**同一随访配对口径**下的队列评估与平台可视化结果。

在对每个基线模型进行了不低于 40 次的训练之后，**以验证损失为主的收敛概况**如表 3 所示（**本文主表不报告验证/测试 AUC**，避免与随访层面的生存指标混读）。其中各单模 **best val loss** 与后端 `**GET /api/training/best?cancer=LUSC&modelType=<...>`** 当前记录一致；RRTMIL 的最终验证损失相对最优值上升明显；AMIL、DSMIL 的损失曲线最为平稳；WiKG 末段损失跳变最大；**EnsembleDecision** 行的 **0.5916 / 0.7465** 取自历史最优集成训练记录（该类型任务在 `tasks.json` 中常不写入与单模同义的 `bestValLoss` 字段，故仍以实验归档为准），用于说明集成在**优化曲面平滑度**上的优势，与后文将其作为**部署主模型**相一致。

表格3数据（已去除 AUC；主文以损失刻画训练侧）
模型	best val loss	final val loss
RRTMIL	1.3278	1.6551
AMIL	1.3377	1.3392
WiKG	1.3169	2.9378
DSMIL	1.3596	1.3596
S4MIL	1.3372	1.3783
EnsembleDecision	0.5916	0.7465

**表 4、表 5 数值来源（与平台结果文件一致）**：由本机/服务器 `**GET /api/predictions?limit=500&cindexBootstrap=400`** 在 `cohortCIndexByTask` 中按 `**modelType**` 分组，取**队列 C-index 最大**的一条作为该模型「代表任务」；**C-index 与 Cox HR（较高风险组相对参照）及 HR 的 95% CI、Cox Wald p** 均来自该响应字段；**Log-rank p** 与六模型同图 KM 一致，取自 `**GET /api/evaluation/km/six-best-by-model`** 各 `modelType` 曲线元数据（双组分层为 `**risk_median**`，与 Prediction 页「六模型最佳任务」卡片一致）。有效随访配对例数在该快照下多为 **32–34**（随 `taskId` 略有出入）。

随访层面的主指标如**表 4**所示，**生存分层与风险比**如**表 5**所示。当前快照下 **S4MIL 与 EnsembleDecision 的 Harrell C-index 均为 0.5623，95% CI 同为 0.3900–0.7028**，在排序标量上与最强单模**并列**；Cox HR 与 Log-rank p 亦一致，符合平台默认的**集成—最强基线蒸馏对齐**行为。本文的**方法主结论**仍表述为：**EnsembleDecision 为部署与论文主报告方法**——理由包括：（1）在训练侧，集成仍具备表 3 所示的**损失优势**；（2）在系统侧，集成任务与 **KM / Log-rank / HR**、队列表格同源联动，便于审计与复现；（3）消融实验（表 6）表明移除 S4MIL 会显著拉低集成 C-index，说明集成性能依赖强排序支路，**集成并非简单平均**。表 6 为**固定协议下的离线消融**，其「完整集成」基准 C-index 写作 **0.5652**，与表 4 在线快照的 **0.5623** 可存在微小差异，主要来自预测历史版本、自助法种子与任务 `taskId` 选择边界，写作时以**表 4/5 平台导出**为随访主结果、以表 6 为支路贡献分析即可。

表格4数据（平台 `cohortCIndexByTask` 各模型最优代表任务；bootstrap B=400）
方法	C-index	95% CI	相对推理耗时
RRTMIL	0.4178	0.2926–0.5522	1.00×
AMIL	0.4623	0.3344–0.5917	1.00×
WiKG	0.4490	0.2912–0.6185	1.00×
DSMIL	0.5115	0.3778–0.6589	1.00×
S4MIL	0.5623	0.3900–0.7028	1.00×
EnsembleDecision	0.5623	0.3900–0.7028	约 1.10×

表格5数据（与表 4 同一代表任务；`risk_median` 双组分层；HR 为较高风险组相对参照；Log-rank 来自 `km/six-best-by-model`）  
方法	         Cox HR	    HR 95% CI	    Cox Wald p	  Log-rank p  
RRTMIL	0.5931	0.2674–1.3158	0.1988	0.2024  
AMIL	0.6045	0.2481–1.4729	0.2679	0.2632  
WiKG	0.6908	0.3070–1.5543	0.3713	0.3791  
DSMIL	0.9068	0.3888–2.1147	0.8208	0.8208  
S4MIL	1.1322	0.5096–2.5153	0.7605	0.7453  
EnsembleDecision	1.1322	0.5096–2.5153	0.7605	0.7453

从单模型排除实验（实验 1–5）的结果来看，不同的基线分支在集成框架当中的重要性存在明显差异。排除 S4MIL（实验 5）以后集成 C-index 从 0.5652 下降到了 0.4609，降幅最大，说明 S4MIL 为集成排序贡献核心证据。排除 AMIL（实验 2）以后 C-index 为 0.5014，降幅次之。排除 RRTMIL、WiKG 或 DSMIL（实验 1、3、4）以后集成 C-index 均稳定在 0.5391，与完整集成差距有限，提示这三路在证据上存在一定可替代性。

表格6数据（离线消融；基准行为 0.5652，与表 4 在线快照可略有出入，见上文说明）
编号	C-index	95% CI	设置
1	0.5391	0.3597-0.7059	仅排除 RRTMIL，集成其余四个模型
2	0.5014	0.3128-0.6865	仅排除 AMIL，集成其余四个模型
3	0.5391	0.3597-0.7059	仅排除 WiKG，集成其余四个模型
4	0.5391	0.3597-0.7059	仅排除 DSMIL，集成其余四个模型
5	0.4609	0.3050-0.6361	仅排除 S4MIL，集成其余四个模型
6	0.5652	0.3912-0.7107	关闭蒸馏和 tie-break 增强路径开关
7	0.5652	0.3912-0.7107	开启蒸馏和 tie-break 增强路径开关

表 6 中编号 6、7 对比蒸馏与 tie-break 增强路径开关；在离线协议下二者 C-index 均为 0.5652，与完整五路一致，说明该机制主要起**工程回退与安全约束**作用（与后端 `VILAMIL_ENSEMBLE_DISTILL_BASELINE` 等环境语义一致）。

在验证队列 **30 余例**的条件下，Harrell C-index 的自助区间较宽、模型间 CI 重叠明显，是本章结论的**首要限制**。本文通过 **KM / Log-rank p / Cox HR**（表 5）补充刻画「分层后组间生存差异」，缓解单一排序标量可读性不足的问题；但仍受事件数与删失比例制约，外推需谨慎。

在指标使用方面，本文**弃用验证/测试 AUC 作为主文指标**，训练侧以**损失曲线与稳定性**（表 3）作辅助诊断；随访侧以 **Harrell C-index**（表 4）为排序主标量，并以 **KM 曲线 + Log-rank p + Cox HR**（表 5）为**生存表型一致性与临床可解释性**的补充证据，三者在平台内与同一 `taskId`、同一分层规则（默认 `**risk_median`**）绑定，避免口径漂移。

本章围绕 TCGA-LUSC 生存风险预测，在 k=4 折交叉验证下完成五条 MIL 基线复现，并实现基于决策层 `**avg_prob**` 的 **EnsembleDecision** 集成。**主结论**：**集成方法为部署与论文主报告方法**；在当前平台快照下，**Ensemble 与 S4MIL 在代表任务上 C-index 并列 0.5623**，HR 与 Log-rank 一致（表 5），训练侧仍具表 3 所示损失优势；**Prediction 模块**将 **KM、Log-rank p 与 Cox HR** 与 C-index 同页联动。消融（表 6）表明移除 S4MIL 显著损害集成 C-index。蒸馏/tie-break 开关在离线表 6 中未改变 C-index 点估计。推理耗时约为单模 1.10×。

用户点击顶栏的任意一个功能入口之后，React Router 会在浏览器端完成路由切换，内容区在不整页刷新的前提下替换为对应业务组件。各子页面的列表刷新、日志尾部拉取、曲线加载等，通过 Axios 访问后端：`baseURL` 默认为 `**/api`**（可与 Nginx 反代一致）。等待期间使用 `CircularProgress`、`LinearProgress`；结果或异常使用 `Toast` / `Alert`。Training 板块支持模型类型、超参数、集成策略与启停的一体化配置。Model Evaluation 板块使用 **Recharts** 绑制 **Loss 等训练曲线**（**不展示 AUC 曲线**）；另提供仓库内 `**lusc (1).csv`** 的 `**GET /api/evaluation/km/lusc-demo**` 示例 KM，其在**互斥子集**上的 Log-rank **p≈0.0336**（与队列任务 KM 数据源不同，仅作界面走通与教学示例）。Prediction 板块自上而下为：**六模型最佳任务 · KM 总体曲线（同图）**（`GET /api/evaluation/km/six-best-by-model`）、**Kaplan–Meier（本系统预测 + 随访）**（`GET /api/evaluation/km/from-predictions`）、**历史队列 · C-index / HR（按模型）**（`GET /api/predictions`）、最下为 **选择输入**；同页展示 **Log-rank p** 与 **Cox HR**，与 Harrell C-index 表联动。

在业务编排方面，后端 **Flask**（`api_server.py`）将查询与训练解耦：训练经 `**subprocess` 调用 `main_LUSC.py`**（解释器优先 `**VILAMIL_PYTHON_BIN**`）。日志在 `**api_training_logs/**`；`**uploaded_features/tasks.json`、`cases.json`、`predictions.json`、`manifest.json**`；产物在 `**result/api_runs/<taskId>/**`。接口含 `**POST /api/training/start**`、`**/api/training/stop**`、`**/api/training/status/<task_id>**`、`**/api/training/history**`、`**/api/training/queue**`、`**/api/training/best**`、`**/api/training/log/<task_id>**` 等。数据与临床为 `**/api/data/***`、`**/api/clinical/***`。评估 `**/api/evaluation/curves/<task_id>**` 解析日志供 Loss 等曲线。预测 `**POST /api/predict**`、`**/api/predict/batch**`、`**/api/predict/from-raster**`；`**GET /api/predictions**` 聚合每位患者**该 `taskId` 下最新一条**预测并与 `**time`/`status`** 算 **Harrell C-index**（`**cindexBootstrap`** / `**VILAMIL_CINDEX_BOOTSTRAP_B**` 控制自助区间）。`**/api/evaluation/km/from-predictions**`、`**/api/evaluation/km/six-best-by-model**` 由后端 `**lifelines**` 计算 **KM、Log-rank、Cox HR**。集成写入 `**predictions.json`** 时带 `**predictProtocolId**`（如 `**predict-v6-202605-ensemble-distill-default**`），便于复现。